/**
 * Auth Layout
 * Layout untuk auth pages (login, register, etc)
 */

import React from 'react';

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
